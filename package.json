{
  "name": "teydra-extensions-registry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-registry.mjs",
    "test": "node --test scripts/*.test.mjs",
    "check": "node scripts/build-registry.mjs --check && npm test",
    "audit:packages": "node scripts/audit-packages.mjs"
  }
}
